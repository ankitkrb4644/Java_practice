package Day_5;
import java.io.*;
public class prac_2 {
    BufferedReader bc = new BufferedReader ( new InputStreamReader(System.in));
    int n = Integer.parse.Int(bc.readLine());
    while(n-- > o){
        char c1 = bc.readLine().trim().charAt(0);
        char c2 = bc.readLine().trim().charAt(0);
    }
}
